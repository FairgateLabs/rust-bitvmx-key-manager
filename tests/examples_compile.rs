// Modules added to ensure that examples compile, (will be checked at cargo test)

// Compile-check keymanager_usage example
#[allow(dead_code)]
mod skeymanager_usage_example {
    include!("../examples/keymanager_usage.rs");
}
